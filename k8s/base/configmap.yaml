apiVersion: v1
kind: ConfigMap
metadata:
  name: aerlix-config
  namespace: aerlix
  labels:
    app.kubernetes.io/name: aerlix-control-plane
    app.kubernetes.io/component: config
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  REDIS_URL: "redis://aerlix-redis:6379/0"
